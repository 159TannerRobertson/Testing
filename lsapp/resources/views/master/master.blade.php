@extends('/layouts.app')

@section('content')
    <h1>Posts Page Test Master</h1>
    {{-- @if(!!count($posts))
        @foreach($posts as $post)
            <div class="well">
                <h3>{{$post->title}}</h3>
            </div>
        @endforeach
    @else
        <p>No posts found</p>
    @endif --}}
@endsection
